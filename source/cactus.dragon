[ main ]
dragon.main()() = {
    [ display startup message ]
    dragon.set("🌵 Running the cactus compiler...! Ouch!%0a;")(message)
    dragon.print.buffer_as_string(message)()

    [ get all user code buffers from context input ]
    dragon.copy(dragon.context.input)(user_codes)
    
    [ larse files ]
    cactus.larse(user_codes)(output, error)
    dragon.error.print_if_occured(error)()
    dragon.jump.bottom(error:occured, @dragon.master_scope)()
}
